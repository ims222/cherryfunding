<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cherryfunding.spring.vo.FundingMapper">
	<select id="list" resultType="funding">
		select * from funding
	</select>
	
	<select id="select" parameterType="int" resultType="funding">
		select * from funding where fnum = #{num}
	</select>
	<insert id="insert" parameterType="funding">
		insert into funding values(#{fnum},#{title},#{content},#{amount},
		0,#{sdate},#{edate},#{category},
		#{confirm},#{aid},#{addr},sysdate,#{id},0)
	</insert>
	<update id="update" parameterType="funding">
		update funding set title=#{title},content=#{content},amount=#{amount},camout=#{camout},
		sdate=#{sdate},edate=#{edate},category=#{category},confirm=#{confirm},aid=#{aid},
		addr=#{addr},id=#{id} where fnum = #{num} 
	</update>
	<delete id="delete" parameterType="int">
		delete from funding where fnum=#{num}
	</delete>
	<select id="getMaxNum" resultType="int">
		select NVL(max(fNum), 0) from funding
	</select>
	
	
	
	<!-- 준비중 펀딩 -->
	<select id="prepare" resultType="funding">
		select * from funding
		<![CDATA[where sdate > sysdate order by fnum desc]]>
	</select>
	
	<!-- 종료된 펀딩 -->
	<select id="end" resultType="funding">
		select * from funding
		<![CDATA[where edate < sysdate order by fnum desc]]>
	</select>
</mapper>